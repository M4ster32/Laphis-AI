{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "python -m uvicorn ai-service.src.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyMaxRetries": 5
  }
}
